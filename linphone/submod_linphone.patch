Submodule submodules/linphone contains modified content
diff --git a/submodules/linphone/coreapi/linphonecore.c b/submodules/linphone/coreapi/linphonecore.c
index 5854774f..5a907e6f 100644
--- a/submodules/linphone/coreapi/linphonecore.c
+++ b/submodules/linphone/coreapi/linphonecore.c
@@ -1628,6 +1628,7 @@ static void monitor_network_state(LinphoneCore *lc, time_t curtime){
 	/* only do the network up checking every five seconds */
 	if (last_check==0 || (curtime-last_check)>=5){
 		linphone_core_get_local_ip_for(lc->sip_conf.ipv6_enabled ? AF_INET6 : AF_INET,NULL,result);
+	    ms_message("lnphone_core_get_local_ip = %s",result);
 		if (strcmp(result,"::1")!=0 && strcmp(result,"127.0.0.1")!=0){
 			new_status=TRUE;
 		}else new_status=FALSE;
@@ -4396,6 +4397,7 @@ void linphone_core_set_network_reachable(LinphoneCore* lc,bool_t isReachable) {
 		ms_message("Disabling automatic network state monitoring");
 		lc->auto_net_state_mon=FALSE;
 	}
+    ms_message("linphone_core_set_network_reachable %d", isReachable);
 	set_network_reachable(lc,isReachable, ms_time(NULL));
 }
 
diff --git a/submodules/linphone/coreapi/misc.c b/submodules/linphone/coreapi/misc.c
index ea85f103..b5173c48 100644
--- a/submodules/linphone/coreapi/misc.c
+++ b/submodules/linphone/coreapi/misc.c
@@ -33,15 +33,14 @@ Foundation, Inc., 59 Temple Place - Suite 330, Boston, MA  02111-1307, USA.
 #include <sys/stat.h>
 #include <unistd.h>
 #include <fcntl.h>
+#include <sys/socket.h>
+#include <net/if.h>
+#include <sys/ioctl.h>
 #endif /*_WIN32_WCE*/
 
 #undef snprintf
 #include <ortp/stun.h>
 
-#ifdef HAVE_GETIFADDRS
-#include <net/if.h>
-#include <ifaddrs.h>
-#endif
 
 
 #if !defined(WIN32)
@@ -570,9 +569,203 @@ LinphoneProxyConfig * is_a_linphone_proxy_config(void *user_pointer){
 }
 
 
-#ifdef HAVE_GETIFADDRS
 
-#include <ifaddrs.h>
+#ifdef HAVE_SYS_SOCKIO_H
+#include <sys/sockio.h>
+#endif
+
+#ifdef HAVE_NETINET_IN6_VAR_H
+#include <netinet/in6_var.h>
+#endif
+
+#ifndef max
+#define max(a,b) ((a)>(b)?(a):(b))
+#endif
+
+
+struct ifaddrs {
+    struct ifaddrs *ifa_next;
+    char *ifa_name;
+    unsigned int ifa_flags;
+    struct sockaddr *ifa_addr;
+    struct sockaddr *ifa_netmask;
+    struct sockaddr *ifa_dstaddr;
+    void *ifa_data;
+};
+
+#ifndef ifa_broadaddr
+#define ifa_broadaddr ifa_dstaddr
+#endif
+
+static int
+getifaddrs2(struct ifaddrs **ifap, 
+	    int af, int siocgifconf, int siocgifflags,
+	    size_t ifreq_sz)
+{
+    int ret;
+    int fd;
+    size_t buf_size;
+    char *buf;
+    struct ifconf ifconf;
+    int num, j = 0;
+    char *p;
+    size_t sz;
+    struct sockaddr sa_zero;
+    struct ifreq *ifr;
+
+    struct ifaddrs *start,  **end = &start;
+
+    buf = NULL;
+
+    memset (&sa_zero, 0, sizeof(sa_zero));
+    fd = socket(af, SOCK_DGRAM, 0);
+    if (fd < 0)
+	return -1;
+
+    buf_size = 8192;
+    for (;;) {
+	buf = calloc(1, buf_size);
+	if (buf == NULL) {
+	    ret = ENOMEM;
+	    goto error_out;
+	}
+	ifconf.ifc_len = buf_size;
+	ifconf.ifc_buf = buf;
+
+	/*
+	 * Solaris returns EINVAL when the buffer is too small.
+	 */
+	if (ioctl (fd, siocgifconf, &ifconf) < 0 && errno != EINVAL) {
+	    ret = errno;
+	    goto error_out;
+	}
+	/*
+	 * Can the difference between a full and a overfull buf
+	 * be determined?
+	 */
+
+	if (ifconf.ifc_len < (int)buf_size)
+	    break;
+	free (buf);
+	buf_size *= 2;
+    }
+
+    num = ifconf.ifc_len / ifreq_sz;
+    j = 0;
+    for (p = ifconf.ifc_buf;
+	 p < ifconf.ifc_buf + ifconf.ifc_len;
+	 p += sz) {
+	struct ifreq ifreq;
+	struct sockaddr *sa;
+	size_t salen;
+
+	ifr = (struct ifreq *)p;
+	sa  = &ifr->ifr_addr;
+
+	sz = ifreq_sz;
+	salen = sizeof(struct sockaddr);
+#ifdef HAVE_STRUCT_SOCKADDR_SA_LEN
+	salen = sa->sa_len;
+	sz = max(sz, sizeof(ifr->ifr_name) + sa->sa_len);
+#endif
+#ifdef SA_LEN
+	salen = SA_LEN(sa);
+	sz = max(sz, sizeof(ifr->ifr_name) + SA_LEN(sa));
+#endif
+	memset (&ifreq, 0, sizeof(ifreq));
+	memcpy (ifreq.ifr_name, ifr->ifr_name, sizeof(ifr->ifr_name));
+
+	if (ioctl(fd, siocgifflags, &ifreq) < 0) {
+	    ret = errno;
+	    goto error_out;
+	}
+
+	*end = malloc(sizeof(**end));
+
+	(*end)->ifa_next = NULL;
+	(*end)->ifa_name = strdup(ifr->ifr_name);
+	(*end)->ifa_flags = ifreq.ifr_flags;
+	(*end)->ifa_addr = malloc(salen);
+	memcpy((*end)->ifa_addr, sa, salen);
+	(*end)->ifa_netmask = NULL;
+
+#if 0
+	/* fix these when we actually need them */
+	if(ifreq.ifr_flags & IFF_BROADCAST) {
+	    (*end)->ifa_broadaddr = malloc(sizeof(ifr->ifr_broadaddr));
+	    memcpy((*end)->ifa_broadaddr, &ifr->ifr_broadaddr, 
+		   sizeof(ifr->ifr_broadaddr));
+	} else if(ifreq.ifr_flags & IFF_POINTOPOINT) {
+	    (*end)->ifa_dstaddr = malloc(sizeof(ifr->ifr_dstaddr));
+	    memcpy((*end)->ifa_dstaddr, &ifr->ifr_dstaddr, 
+		   sizeof(ifr->ifr_dstaddr));
+	} else
+	    (*end)->ifa_dstaddr = NULL;
+#else
+	    (*end)->ifa_dstaddr = NULL;
+#endif
+
+	(*end)->ifa_data = NULL;
+
+	end = &(*end)->ifa_next;
+	
+    }
+    *ifap = start;
+    close(fd);
+    free(buf);
+    return 0;
+  error_out:
+    close(fd);
+    free(buf);
+    errno = ret;
+    return -1;
+}
+
+static int
+getifaddrs(struct ifaddrs **ifap) 
+{
+    int ret = -1;
+    errno = ENXIO;
+#if defined(AF_INET6) && defined(SIOCGIF6CONF) && defined(SIOCGIF6FLAGS)
+    if (ret)
+	ret = getifaddrs2 (ifap, AF_INET6, SIOCGIF6CONF, SIOCGIF6FLAGS,
+			   sizeof(struct in6_ifreq));
+#endif
+#if defined(HAVE_IPV6) && defined(SIOCGIFCONF)
+    if (ret)
+	ret = getifaddrs2 (ifap, AF_INET6, SIOCGIFCONF, SIOCGIFFLAGS,
+			   sizeof(struct ifreq));
+#endif
+#if defined(AF_INET) && defined(SIOCGIFCONF) && defined(SIOCGIFFLAGS)
+    if (ret)
+	ret = getifaddrs2 (ifap, AF_INET, SIOCGIFCONF, SIOCGIFFLAGS,
+			   sizeof(struct ifreq));
+#endif
+    return ret;
+}
+
+static void
+freeifaddrs(struct ifaddrs *ifp)
+{
+    struct ifaddrs *p, *q;
+    
+    for(p = ifp; p; ) {
+	free(p->ifa_name);
+	if(p->ifa_addr)
+	    free(p->ifa_addr);
+	if(p->ifa_dstaddr) 
+	    free(p->ifa_dstaddr);
+	if(p->ifa_netmask) 
+	    free(p->ifa_netmask);
+	if(p->ifa_data)
+	    free(p->ifa_data);
+	q = p;
+	p = p->ifa_next;
+	free(q);
+    }
+}
+
+
 static int get_local_ip_with_getifaddrs(int type, char *address, int size)
 {
 	struct ifaddrs *ifp;
@@ -584,15 +777,17 @@ static int get_local_ip_with_getifaddrs(int type, char *address, int size)
 	}
 
 	for (ifp = ifpstart; ifp != NULL; ifp = ifp->ifa_next) {
+		ms_error("getifaddr(%p:%d:%d:%s:%d)" , ifp->ifa_addr, ifp->ifa_addr->sa_family, type, ifp->ifa_name, ifp->ifa_flags);
 		if (ifp->ifa_addr && ifp->ifa_addr->sa_family == type
 			&& (ifp->ifa_flags & IFF_RUNNING) && !(ifp->ifa_flags & IFF_LOOPBACK))
 		{
+		    ms_error("checking getifaddr(%s:%d)" , ifp->ifa_name, ifp->ifa_flags);
 			getnameinfo(ifp->ifa_addr,
 						(type == AF_INET6) ?
 						sizeof(struct sockaddr_in6) : sizeof(struct sockaddr_in),
 						address, size, NULL, 0, NI_NUMERICHOST);
 			if (strchr(address, '%') == NULL) {	/*avoid ipv6 link-local addresses */
-				/*ms_message("getifaddrs() found %s",address);*/
+				ms_message("getifaddrs() found %s",address);
 				ret++;
 				break;
 			}
@@ -601,7 +796,6 @@ static int get_local_ip_with_getifaddrs(int type, char *address, int size)
 	freeifaddrs(ifpstart);
 	return ret;
 }
-#endif
 
 
 static int get_local_ip_for_with_connect(int type, const char *dest, char *result){
@@ -634,7 +828,7 @@ static int get_local_ip_for_with_connect(int type, const char *dest, char *resul
 	}
 	err=connect(sock,res->ai_addr,res->ai_addrlen);
 	if (err<0) {
-		ms_error("Error in connect: %s",strerror(errno));
+		ms_error("Error in connect(%s): %s", dest, strerror(errno));
  		freeaddrinfo(res);
  		close_socket(sock);
 		return -1;
@@ -666,7 +860,6 @@ static int get_local_ip_for_with_connect(int type, const char *dest, char *resul
 
 int linphone_core_get_local_ip_for(int type, const char *dest, char *result){
 	strcpy(result,type==AF_INET ? "127.0.0.1" : "::1");
-#ifdef HAVE_GETIFADDRS
 	if (dest==NULL) {
 		/*we use getifaddrs for lookup of default interface */
 		int found_ifs;
@@ -679,7 +872,6 @@ int linphone_core_get_local_ip_for(int type, const char *dest, char *result){
 			return -1;
 		}
 	}
-#endif
 	/*else use connect to find the best local ip address */
 	if (type==AF_INET)
 		dest="87.98.157.38"; /*a public IP address*/
diff --git a/submodules/linphone/coreapi/sal_eXosip2.c b/submodules/linphone/coreapi/sal_eXosip2.c
index a89a8a13..7baa1b6f 100644
--- a/submodules/linphone/coreapi/sal_eXosip2.c
+++ b/submodules/linphone/coreapi/sal_eXosip2.c
@@ -396,6 +396,12 @@ int sal_listen_port(Sal *ctx, const char *addr, int port, SalTransport tr, int i
 		return -1;
 	}
 	err=eXosip_listen_addr(proto, addr, port, ipv6 ?  PF_INET6 : PF_INET, is_secure);
+    if (err != 0) {
+		ms_error("eXosip_listen_addr(%s:%d) err=%d", addr,port,err);
+    }
+    else {
+		ms_error("************** eXosip_listen_addr(%s:%d) **************", addr,port);
+    }
 	ctx->running=TRUE;
 	return err;
 }
Submodule oRTP contains modified content
diff --git a/submodules/linphone/oRTP/src/rtpsession_inet.c b/submodules/linphone/oRTP/src/rtpsession_inet.c
index b224f5a..bad5f83 100644
--- a/submodules/linphone/oRTP/src/rtpsession_inet.c
+++ b/submodules/linphone/oRTP/src/rtpsession_inet.c
@@ -523,7 +523,8 @@ int rtp_session_set_dscp(RtpSession *session, int dscp){
 		tos = (session->dscp << 2) & 0xFC;
 		switch (session->rtp.sockfamily) {
 			case AF_INET:
-			retval = setsockopt(session->rtp.socket, IPPROTO_IP, IP_TOS, (SOCKET_OPTION_VALUE)&tos, sizeof(tos));
+			//retval = setsockopt(session->rtp.socket, IPPROTO_IP, IP_TOS, (SOCKET_OPTION_VALUE)&tos, sizeof(tos));
+			retval = 0;
 			break;
 #ifdef ORTP_INET6
 		case AF_INET6:
